[package]
name = "rustle"
version = "0.1.0"
edition = "2021"
description = "Minimal TCP stream proxy passthrough"
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["full"] }

[profile.release]
opt-level = "s"
lto = true
strip = true
